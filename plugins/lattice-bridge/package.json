{
  "name": "lattice-bridge",
  "version": "0.1.0",
  "description": "OpenCode plugin that streams session events to the Lattice HTTP bridge",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "keywords": [
    "lattice",
    "opencode",
    "plugin"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.2.1",
    "typescript": "^5.4.0"
  }
}
